import { styled } from "@mui/material/styles";
import Fab from "@mui/material/Fab";

export const NewChatButtonWrapper = styled("div")({
  padding: "15px 20px",
  display: "flex",
  justifyContent: "flex-end",
});

export const NewChatButton = styled(Fab)({});
